# Gemini Markdown File

This file is used to provide context to the Gemini AI.

## Project Structure

*   `src`: Contains the source code of the application.
*   `src/components`: Contains the reusable components.
*   `src/pages`: Contains the pages of the application.
*   `src/styles`: Contains the global styles of the application.

## Available Scripts

*   `npm run dev`: Runs the application in development mode.
*   `npm run build`: Builds the application for production.
*   `npm run serve`: Serves the production build.

## Project Dependencies

*   `react`: A JavaScript library for building user interfaces.
*   `react-dom`: A package for working with the DOM in React.
*   `react-router-dom`: A package for routing in React applications.
*   `graphql`: A query language for APIs.
*   `@apollo/client`: A comprehensive state management library for JavaScript that enables you to manage both local and remote data with GraphQL.

## GraphQL Schema

The GraphQL schema is located at `graphql.schema.json`.

## Available Queries

*   `useGetPokemonsQuery`: A query to get a list of pokemons.

## Available Mutations

There are no mutations available in this project.

## Available Subscriptions

There are no subscriptions available in this project.

## Project Configuration

The project is configured with a `.graphqlconfig` file.

## Code Generation

The project uses GraphQL Code Generator to generate types and hooks from the GraphQL schema. The configuration is located in `codegen.ts`.

## Linter

The project uses ESLint for linting. The configuration is located in `.eslintrc.cjs`.

## Gitignore

The project has a `.gitignore` file that ignores the `node_modules` directory, the `dist` directory, and other files.

## package.json

The `package.json` file contains the project's dependencies and scripts.

## tsconfig.json

The `tsconfig.json` file contains the TypeScript configuration for the project.